  clusters:
    - clusterName: cluster1
      clusterRegion: westus2
      vnetName: shoDNSvnet
      clusterMeta:
      - VMSSgroup: rally
        CBServerGroup: one
        nodeCount: 1
        nodeType: Standard_DS4_v2
        diskSize: 32
        services:
          - data